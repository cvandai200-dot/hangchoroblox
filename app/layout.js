import './globals.css'

export const metadata = {
  title: 'Hàng Chờ Roblox - Giveaway',
  description: 'Tham gia hàng chờ Giveaway Robux',
}

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  )
}